#include "main.h"
/**
*_abs - output function
*@n: An input number
* Description:'function to calaculate the abulote value of anumber'
*
* Return: feturn the absulte value
*/
int _abs(int n)
{
	if (n < 0)
		return (n * -1);
	else
		return (n);
}
