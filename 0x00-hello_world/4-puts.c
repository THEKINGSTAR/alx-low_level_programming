#include <stdio.h>
#include <string.h>
/*using the puts function in main*/
int main(void)
{char str1[90];
strcpy(str1, "\"Programming is like building a multilingual puzzle");
puts(str1);
return (0);
}
